#ifndef PAGER_H
#define PAGER_H

#define PAGER_DEFAULT 0
#define PAGER_USE_FORCE (1<<0)
 /* only page output that is
  * marked with thte pager flag -- not supported */
#define PAGER_USE_PAGER_FLAG (1<<1)

#endif
